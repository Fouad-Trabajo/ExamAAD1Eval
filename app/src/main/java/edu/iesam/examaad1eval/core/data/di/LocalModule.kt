package edu.iesam.examaad1eval.core.data.di

import androidx.room.Room

class LocalModule {
/**
    val db = Room.databaseBuilder(
        applicationContext,
        AppDatabase::class.java, "database-name"
    ).build()
    */
}